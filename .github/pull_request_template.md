<!-- Read this before writing more: this repository accepts EDITORIAL fixes only.

`yellowpaper.md` is a generated mirror, built from FLOP's engineering repository where the
parameter- and citation-drift gates live. A change of meaning cannot carry the decision record,
the regenerated parameter table, or the gate run that a spec change requires — so it is closed
with a pointer to the issue templates, however correct it is, and the next publish would
overwrite it anyway. `Triage pull requests` does this automatically.

Changing a requirement, parameter, bound, or argument? Stop, and open a
[Challenge or Ambiguity issue](../../issues/new/choose) instead. That is where it gets a real
response, and accepted findings are credited in ERRATA.md and in the decision record. -->

## What

<!-- The typo, broken link, malformed table, or rendering fix. One or two sentences. -->

## Where

<!-- Version and section — "v0.5 §3.4", or a requirement ID. Not "the slashing section":
that ages badly across releases. -->

## Checks

- [ ] Editorial only — no requirement, parameter, bound, or argument changes meaning
- [ ] Cites version and section above
- [ ] Not already tracked in Appendix E, or says why the Appendix E entry is insufficient
